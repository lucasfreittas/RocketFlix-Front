import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  text-align: center;
  font-weight: normal;
  font-size: 15px;
  color:${({theme}) => theme.COLORS.PINK};
`;